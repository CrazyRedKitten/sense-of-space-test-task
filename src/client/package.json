{
  "name": "client",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Aleksandr Shchilkin <crazyredkitten@icloud.com>",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^7.32.0",
    "eslint-config-google": "^0.14.0",
    "parcel-bundler": "^1.12.5"
  },
  "scripts": {
    "prebuild": "yarn global add parcel-bundler && yarn install",
    "dev": "yarn prebuild && parcel index.html",
    "build": "yarn prebuild && parcel build index.html --out-dir ../server/dist"
  },
  "prettier": {
    "singleQuote": true,
    "tabWidth": 2,
    "bracketSpacing": false,
    "trailingComma": "all"
  },
  "dependencies": {
    "three": "^0.131.1"
  }
}